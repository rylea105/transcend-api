- name: Provision an EC2 Instances
  import_tasks: nginx.yml

- name: Curl Node 8.X and install nodejs
  shell: curl --silent --location https://rpm.nodesource.com/setup_8.x | sudo bash - | yum -y install nodejs

- name: Install install build tools
  shell: sudo yum install gcc-c++ make