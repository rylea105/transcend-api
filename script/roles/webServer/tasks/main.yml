  - name: Install epel-release
    yum:
        name: epel-release
        state: present

  - name: Install Nginx
    yum:
        name: nginx
        state: latest
    notify: Start Nginx

  - name: Install Git
    yum:
       name: git
       state: latest

  - name: Start Nginx
    service:
            name=nginx
            state=started
            enabled=True

  - name: copy nginx.conf
    copy:
       src=../config/nginx.conf
       dest=/etc/nginx/conf.d/
